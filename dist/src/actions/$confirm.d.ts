import { ActionMethodState, Parameters } from "../lib/runner";
export declare function $confirm(action: string, parameters: Parameters, state: ActionMethodState): Promise<boolean>;
export default $confirm;
