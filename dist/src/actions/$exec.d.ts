import { ActionMethodState, Parameters } from "../lib/runner";
export declare function $exec(action: string, parameters: Parameters, state: ActionMethodState): Promise<string[]>;
export default $exec;
